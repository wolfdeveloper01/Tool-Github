name: Notificação de Push no Repositório

on:
  push:
    branches:
      - main  # Ou qualquer outra branch que você desejar

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Enviar notificação para o Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          # Obter informações do commit
          REPO_URL="https://github.com/${{ github.repository }}"
          COMMIT_URL="${REPO_URL}/commit/${{ github.sha }}"
          COMMIT_AUTHOR="${{ github.actor }}"
          COMMIT_MESSAGE="${{ github.event.head_commit.message }}"

          # Criar mensagem com formatação usando printf para garantir as quebras de linha
          MESSAGE=$(printf "🚀 *Novo Push no Repositório!*\n\n")
          MESSAGE+=$(printf "*Repositório:* [${{ github.repository }}](${REPO_URL})\n")
          MESSAGE+=$(printf "*Autor do Commit:* ${COMMIT_AUTHOR}\n")
          MESSAGE+=$(printf "*Mensagem do Commit:* ${COMMIT_MESSAGE}\n")
          MESSAGE+=$(printf "*Commit:* [Ver Commit](${COMMIT_URL})")

          # Enviar mensagem formatada para o Telegram
          curl -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id=${TELEGRAM_CHAT_ID} \
            -d parse_mode=Markdown \
            -d text="$MESSAGE"
